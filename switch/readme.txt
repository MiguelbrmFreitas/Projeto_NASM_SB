Para compilar esse código (no Windows):
1. digite no terminal o comando: nasm -f win32 switchCase.asm
2. digite no terminal o comando: gcc switchCaseMain.c switchCase.obj -o switchCase
3. digite no terminal o comando: switchCase

obs: o arquivo .asm e .c devem estar na mesma pasta.

